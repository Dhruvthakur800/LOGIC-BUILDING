// Program: Print Pascal’s Triangle using Recursion
#include <stdio.h>
int factorial(int n) {
    if(n==0) return 1;
    return n*factorial(n-1);
}
int combination(int n,int r) {
    return factorial(n)/(factorial(r)*factorial(n-r));
}
void printPascal(int n) {
    for(int i=0;i<n;i++){
        for(int j=0;j<=i;j++){
            printf("%d ", combination(i,j));
        }
        printf("\n");
    }
}
int main() {
    int n;
    printf("Enter number of rows: ");
    scanf("%d",&n);
    printPascal(n);
    return 0;
}
