// Program: Nested Recursion Example
#include <stdio.h>
int fun(int n) {
    if(n > 100) return n - 10;
    return fun(fun(n+11));
}
int main() {
    int n;
    printf("Enter a number: ");
    scanf("%d",&n);
    printf("Result = %d\n", fun(n));
    return 0;
}
